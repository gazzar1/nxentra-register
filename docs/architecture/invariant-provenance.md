# Invariant provenance index

Where a code comment or docstring once named the review round or pull request that discovered an invariant, the comment now states only the invariant; this index keeps the provenance. Roadmap labels (A-numbers, A3-PR3 and the like) stay in the code because NEXT_TASKS and the ADRs cross-reference them.

Generated with the M2 comment-provenance cleanup (2026-09-24) from the edits themselves. Line numbers are those at the time of the cleanup and will drift; the invariant statement is the durable key.

| File | Line (2026-09-24) | Invariant | Provenance removed from the comment | Roadmap label kept |
|---|---|---|---|---|
| `accounting/commands.py` | 195 | translate_posted_journal_invalid is the public-boundary translation for the raise-through pattern, applied ABOVE @transaction.atomic so PostedJournalInvalid rolls back the entire owning transaction before translation. | A3-PR2 correction (Codex P2 finding 1) | A3-PR2 |
| `accounting/commands.py` | 265 | A request_id replay marks the reuse so the caller can tell 'this request MINTED the entry' from 'this request replayed one'; creation-keyed side-band writes must be creator-only. | A5-PR4a (Codex PR #134 round-4 P2) | A5-PR4a |
| `accounting/commands.py` | 1212 | The manual journal door may never edit or clear an entry whose CURRENT source stamp is system-owned (provider flows persist failed journals as INCOMPLETE drafts carrying join-load-bearing provenance). | (round-8 P1) | A5-PR4a |
| `accounting/commands.py` | 1346 | The journal-update event key must include more than the changes hash: an edit cycle (A→B, B→A, A→B) would otherwise reproduce the first update's key and dedupe a real change into silence. | Codex PR #134 round-5 P2 | — |
| `accounting/commands.py` | 1551 | Journal completion idempotency: a TRUE RETRY (same content digest, no UPDATED event after the stored SAVED_COMPLETE) reuses the stored event's own key; a FRESH completion folds the lifetime UPDATED count into the key so an edit-and-revert cycle never re-collides. | Key rules (Codex PR #134 rounds 1-3) | — |
| `accounting/commands.py` | 1696 | The header FX rate stamped on a journal must be the rate that actually converted its lines (convert-or-quarantine), never the 1.0 aggregate default. | PR #34 | A142 |
| `accounting/commands.py` | 1805 | A 1.0 line rate for a foreign pair with no stored rate quarantines the post until a rate exists; mirrors build_journal_entry. | PR #33 | — |
| `accounting/commands.py` | 4617 | Never book a foreign amount at a guessed 1:1 — refuse loudly (the missing-rate refusal class; operator-safety Rule 2). | PR #33 | A203 |
| `accounting/policies.py` | 174 | can_change_account_type must scan the stored posted-journal EVENTS themselves, not projection state (rows or applied-markers), because an unmaterialized posted event or a marker-with-partial-application can reference the account with no row present; freezing the type on any stored payload reference prevents a later rebuild from reinterpreting/quarantining valid history under the new type. | (Codex rounds 1+3 P1) | — |
| `accounting/policies.py` | 204 | In the durable-evidence scan, a malformed payload SHAPE (non-dict payload, non-list lines container, non-dict line entry) must fail closed exactly like a payload-resolution exception — 'no readable references' is not 'no references', since a later repair/rebuild could reveal a hidden reference to the account. | Codex round-4 P2: | — |
| `accounting/posted_journal_apply.py` | 111 | QUARANTINE_MESSAGE_SIGNATURE is the framework-owned marker that tells a QUARANTINE log (event consumed without applying) from an OPERATIONAL halt log, so legacy operator-resolved halts never eclipse an applied journal in the fold disposition. | (round 16) | — |
| `accounting/posted_journal_apply.py` | 275 | is_deferrable_apply_verdict is THE single defer predicate shared by every verdict consumer; verdict symmetry must include the lag disposition or a batch caller reports replayable lag as corruption / refuses a backup of it. | Codex round-1 P2: | — |
| `accounting/posted_journal_apply.py` | 289 | An entry is pending materialization iff a payload-verified JOURNAL_ENTRY_POSTED event sits earlier in the stream and is not yet consumed by the JournalEntry read model (the deferred-post case). | (Codex round-5 P1) | — |
| `accounting/posted_journal_apply.py` | 292 | Pending materialization is decided from the posted EVENT and its applied-marker, never from row existence, because the lifecycle materializes a DRAFT row before the post and a row check would let lifecycle events consume against draft lines the retried post recreates. | (Codex round-9 P1) | — |
| `accounting/posted_journal_apply.py` | 300 | Candidate prior posts are matched by PAYLOAD identity as well as aggregate metadata in BOTH storage strategies (inline data and payload_ref__payload), because external ingest carries a different aggregate_id and >64 KiB payloads store {} inline. | (Codex rounds 6+7 P2) | — |
| `accounting/posted_journal_apply.py` | 303 | Entry identity comparison is canonical-UUID, not stored text; the exact-match query arms are only a fast path and a bounded canonical scan of prior posted events decides when they yield no verified candidate. | (Codex round-10 P2) | — |
| `accounting/posted_journal_apply.py` | 328 | The pending-evidence caller must keep scanning past consumed matches: a stream can carry the same entry id twice and a consumed earlier post must not hide a later deferred REPOST; pending wins if ANY matching prior lacks the marker. | (Codex round-20 P1) | — |
| `accounting/posted_journal_apply.py` | 340 | Only documented payload-integrity failures (IntegrityError, ValueError) negate pending evidence; a transient database error must propagate as a retryable halt instead of erasing evidence and letting a sibling no-op-consume. | (Codex round-21) | — |
| `accounting/posted_journal_apply.py` | 377 | Account-creation evidence must be verified by PAYLOAD (the account_public_id AccountProjection materializes), not aggregate_id alone, or a foreign/corrupted event would make a reference defer forever instead of quarantining. | (Codex round-1 P2) | — |
| `accounting/posted_journal_apply.py` | 394 | The account evaluator catches only integrity failures (not pending evidence); transient DB errors propagate as retryable halts rather than terminally quarantining a valid event. | (Codex round-21) | — |
| `accounting/posted_journal_apply.py` | 416 | Account candidates are queried by payload identity as well as aggregate metadata in both storage strategies (the same rule as the entry probe), so an aggregate-only query cannot cause a valid posted journal to be terminally quarantined instead of deferred. | Codex round-8 P2 — | — |
| `accounting/posted_journal_apply.py` | 421 | Account identity comparison is canonical; exact-match arms are a fast path backed by a bounded canonical-scan fallback, with payload/materializability verification remaining the authority. | (Codex round-10 P2) | — |
| `accounting/posted_journal_apply.py` | 428 | Materializability has two layers — static (creation fields AccountProjection.handle subscripts unconditionally must be present non-empty strings) and dynamic (evidence counts only while the ACCOUNT read model has not consumed the prior event) — because statically re-predicting the projection's write logic is an unwinnable arms race. | Codex rounds 2+3 P2 — | A41 |
| `accounting/posted_journal_apply.py` | 434 | Once the account read model's applied-marker exists, the stale-read rule re-resolves the row: concurrently committed means resolved and the retry succeeds; marker-present-with-row-absent can never materialize and is terminal, not evidence. | round-4 | A41 |
| `accounting/posted_journal_apply.py` | 444 | Same narrowing in the account-evidence probe: only documented payload-integrity failures negate evidence; transient DB errors propagate as retryable halts. | (Codex round-21) | — |
| `accounting/posted_journal_apply.py` | 480 | The canonical-scan fallback catches noncanonical stored UUID spellings the exact-match arms miss; it is bounded by the company's account-event count and runs only on the failure path with no fast-path hit. | (Codex round-10 P2) | — |
| `accounting/posted_journal_apply.py` | 515 | Event-fold readers (report views, AccountBalance verification) fold only what the apply boundary actually accepted into the balances; folding a quarantined or lag-deferred event misstates reports and produces false integrity mismatches. | Codex round-12 P1 — | — |
| `accounting/posted_journal_apply.py` | 519 | Acceptance is decided in two ordered layers: the PERSISTED disposition (an unresolved ProjectionFailureLog) wins over the live verdict — current validity is not historical acceptance — and only events with no failure record fall through to re-evaluation. | (Codex round-13 P1) | A105 |
| `accounting/posted_journal_apply.py` | 537 | Only a SELF-HEALED failure log counts as historical acceptance, keyed on the persisted resolution kind (the SELF_HEALED_RESOLUTION_NOTE sentinel mark_resolved refuses to reproduce); a manual resolution is an operator claim, and nullable SET_NULL resolved_by is not a reliable kind marker. | — Codex rounds 14+15 P1 | — |
| `accounting/posted_journal_apply.py` | 542 | Batch folds pass excluded_ids from excluded_posted_event_ids so they pay ONE disposition query instead of one per event, and share a per-request facts_cache for account lookups. | (Codex round-14 P2) | — |
| `accounting/posted_journal_apply.py` | 555 | The disposition rule is scoped to the BALANCE projection's failure logs: every reader mirrors account_balance amount semantics, and an unrelated consumer's operational failure must not eclipse a journal the balance projection actually applied. | (Codex round-15 P1) | — |
| `accounting/posted_journal_apply.py` | 562 | Per-log disqualification: unresolved; resolved-but-not-self-healed carrying the quarantine signature; and resolved-but-not-self-healed operational failures ONLY when no account_balance applied-marker exists — legacy compatibility without a migration, since marker-present means the retry landed the amounts. | Codex round-16 P1 — | — |
| `accounting/posted_journal_apply.py` | 607 | Memo classification for payload readers is account-derived (a line is memo iff its RESOLVED account is memo), exactly as the invariant and balance consumers decide; trusting the raw is_memo_line flag disagrees with accepted journals and projected balances whenever a historical/foreign flag lies. | Codex round-11 P1 — | — |
| `accounting/posted_journal_apply.py` | 628 | Reader lookups use the canonical account id of a payload line: accepted payloads may spell UUIDs in any equivalent form and reader dictionaries key on canonical str(public_id), so a raw-text lookup would silently drop a line's movement from reports and verification while the balances include it. | (Codex round-18 P1) | — |
| `accounting/posted_journal_apply.py` | 654 | Identity comparisons in this module are ALWAYS canonical (lowercase-hyphenated UUID spelling), because equivalent spellings are accepted inputs everywhere else. | Codex round-10 P2: | — |
| `accounting/posted_journal_apply.py` | 670 | journal_entry.reversed lifecycle ordering: both entry references must be well-formed UUIDs, and while either referenced entry's own posted event is pending materialization the reversal must DEFER, or the handler would silently no-op and the retried posts could never receive their reversal. | (Codex round-5 P1) | D5 |
| `accounting/posted_journal_apply.py` | 690 | With no pending post left to wait for, both entry rows must EXIST before the reversal handler runs; a reversal over a terminally quarantined post (marker present, row absent) quarantines visibly instead of flipping the original to REVERSED with no reversal row or balance movements. | Codex round-11 P1: | — |
| `accounting/posted_journal_apply.py` | 705 | Row existence is not enough for a reversal: both rows must have reached POSTED (REVERSED accepted for idempotent replays), because the lifecycle leaves a DRAFT row under the same id and a reversal over a quarantined post would flip a draft with no balance movements behind it. | Codex round-12 P1: | — |
| `accounting/posted_journal_apply.py` | 728 | journal_entry.deleted lifecycle ordering (same rule as the reversed door): while the target's own posted event is pending materialization the delete defers, so the status probe decides against the materialized row instead of waving the delete through as a no-op. | Codex round-5 P1, | — |
| `accounting/posted_journal_apply.py` | 750 | journal_line_analysis.set lifecycle ordering: the backfill emits it after its journal's post, so a well-formed entry reference whose post is pending materialization DEFERS (a no-op consume would leave the retried post's lines permanently untagged) and a malformed reference quarantines. | (Codex round-6 P1) | — |
| `projections/account_balance.py` | 156 | In _apply_line, memo classification is taken from the RESOLVED ACCOUNT (account.is_memo_account), the same authority rule the canonical invariant applies; the payload is_memo_line flag is historical metadata, so a payload whose flag disagrees with the account can never be validated under one classification and materialized under another (a financial line silently vanishing from balances). | (Codex round-2 P1) | A3-PR3 |
| `projections/account_balance.py` | 347 | In verify_all_balances, memo classification during replay is account-derived (memo_account_public_ids + line_is_memo), never the raw payload flag, so the replayed expected totals classify lines exactly as the projected balances did. | (Codex round-11 P1) | A3-PR3 |
| `projections/account_balance.py` | 361 | verify_all_balances replays only the posted events the apply boundary ACCEPTS (posted_event_accepted_for_apply); a quarantined event is deliberately absent from balances, and folding it into expected totals would report a false integrity mismatch. | (Codex round-12 P1) | A3-PR3 |
| `projections/account_balance.py` | 371 | The replay aggregation key uses the canonical account identity (canonical_line_account_id) because accepted payloads may spell UUIDs in any equivalent form and the comparison keys on str(public_id); a non-canonical key would split one account's totals and produce spurious mismatches. | Codex round-18 P1: | — |
| `projections/base.py` | 357 | Self-heal stamps ALL failure rows for the (company, projection, event) key — manually-resolved ones included — as SELF-HEALED; resolved_by NULL is the persisted mark of a genuine successful re-apply. | A3-PR3 (Codex round-14 P1) | A3-PR3 |
| `projections/base.py` | 448 | The deferred-event bookmark rewind sits in a `finally` so a propagating exception from an except-branch's own writes cannot skip it and leave a later success's advanced bookmark hiding a deferred event. | A5-PR1b (PR #133 Codex P1) | A5-PR1b |
| `projections/failure_log_views.py` | 210 | Operator resolution of a projection failure must route through ProjectionFailureLog.mark_resolved — the single place that guarantees an operator resolution cannot reproduce the framework's self-heal signature. | (Codex round-15 P1) | A3-PR3 |
| `projections/failure_log_views.py` | 311 | ImportRejectedRow list view gates on reports.view because raw_row carries financial import evidence — same read permission as every other report/audit view. | (Codex P2) | — |
| `projections/failure_log_views.py` | 373 | ImportRejectedRow resolve view enforces the same reports.view read gate as the list view, because its response echoes raw_row financial evidence — an admin with reports.view revoked must not read it via resolve either. | (Codex round-2 P2) | — |
| `projections/models.py` | 182 | AccountBalance.verify_balance classifies memo lines from the account (memo_account_public_ids), not from the raw payload flag, so verification agrees with the apply boundary and balance consumers whenever the flag lies. | (Codex round-11 P1) | A3-PR3 |
| `projections/models.py` | 199 | The balance verification fold includes only apply-accepted journal_entry.posted events (posted_event_accepted_for_apply), mirroring AccountBalanceProjection.verify_all_balances, so failed/excluded posts never inflate expected balances. | (Codex round-12 P1) | A3-PR3 |
| `projections/models.py` | 205 | Line-to-account matching in the verification fold uses canonical_line_account_id (the canonical identity), never a raw or alternate id field, so lines are attributed to exactly one account representation. | Codex round-18 P1: | — |
| `projections/models.py` | 1200 | SELF_HEALED_RESOLUTION_NOTE is a framework-owned sentinel written only by BaseProjection.process_pending on a genuine successful re-apply; the event-fold acceptance filter keys on this persisted resolution kind, and mark_resolved must never reproduce it. | (Codex round-15 P1) | — |
| `projections/models.py` | 1323 | ProjectionFailureLog.mark_resolved (operator action) must never be able to produce the framework's SELF-HEALED signature: a colliding note is replaced and an empty note gets an explicit operator marker, because resolved_by is a nullable SET_NULL reference and cannot serve as the kind marker. | (Codex round-15 P1) | A3-PR3 |
| `projections/period_balance.py` | 167 | Memo-account classification is decided from the RESOLVED Account record (the canonical invariant's authority rule), never from the raw event payload flag; PeriodBalance mirrors AccountBalanceProjection._apply_line so both projections skip memo accounts on the same authority. | (Codex round-2 P1) | A3-PR3 |
| `projections/subledger_balance.py` | 128 | Memo-line classification in the subledger projection is derived from the RESOLVED account (the canonical invariant's authority rule); the payload is_memo_line flag is only a fallback when no account resolves, which validated posted events never hit. | (Codex round-2 P1) | A3-PR3 |
| `projections/views.py` | 242 | Memo classification for event-fold readers comes from the RESOLVED ACCOUNT, never the raw payload flag, so reports agree with the accepted journal and projected balances. | (Codex round-11 P1) | A3-PR3 |
| `projections/views.py` | 258 | Event-fold readers fold only events the apply boundary ACCEPTS; a quarantined/deferred event is not in canonical balances and folding it would misstate the report. | (Codex round-12 P1) | A3-PR3 |
| `projections/views.py` | 728 | Memo classification for event-fold readers comes from the RESOLVED ACCOUNT, never the raw payload flag, so reports agree with the accepted journal and projected balances. | (Codex round-11 P1) | A3-PR3 |
| `projections/views.py` | 744 | Event-fold readers fold only events the apply boundary ACCEPTS; a quarantined/deferred event is not in canonical balances and folding it would misstate the report. | (Codex round-12 P1) | A3-PR3 |
| `projections/views.py` | 1245 | Memo classification for event-fold readers comes from the RESOLVED ACCOUNT, never the raw payload flag, so reports agree with the accepted journal and projected balances. | (Codex round-11 P1) | A3-PR3 |
| `projections/views.py` | 1261 | Event-fold readers fold only events the apply boundary ACCEPTS; a quarantined/deferred event is not in canonical balances and folding it would misstate the report. | (Codex round-12 P1) | A3-PR3 |
| `projections/views.py` | 2177 | Memo classification for event-fold readers comes from the RESOLVED ACCOUNT, never the raw payload flag, so reports agree with the accepted journal and projected balances. | (Codex round-11 P1) | A3-PR3 |
| `projections/views.py` | 2193 | Event-fold readers fold only events the apply boundary ACCEPTS; a quarantined/deferred event is not in canonical balances and folding it would misstate the report. | (Codex round-12 P1) | A3-PR3 |
| `projections/views.py` | 3493 | Memo classification for event-fold readers comes from the RESOLVED ACCOUNT, never the raw payload flag, so reports agree with the accepted journal and projected balances. | (Codex round-11 P1) | A3-PR3 |
| `projections/views.py` | 3509 | Event-fold readers fold only events the apply boundary ACCEPTS; a quarantined/deferred event is not in canonical balances and folding it would misstate the report. | (Codex round-12 P1) | A3-PR3 |
| `projections/views.py` | 3742 | The recent-activity widget slices the ten newest ACCEPTED events after the acceptance filter (iterate newest-first, stop at ten accepted); slicing stored events first would let quarantined events shrink or empty the widget while older accepted journals exist. | Codex round-18 P2: | — |
| `projections/views.py` | 3756 | Memo classification for event-fold readers comes from the RESOLVED ACCOUNT, never the raw payload flag, so reports agree with the accepted journal and projected balances. | (Codex round-11 P1) | A3-PR3 |
| `projections/views.py` | 3771 | Event-fold readers fold only events the apply boundary ACCEPTS; a quarantined/deferred event is not in canonical balances and folding it would misstate the report. | (Codex round-12 P1) | A3-PR3 |

Total: 69 provenance tags removed across 10 files. Every change was verified as comment/docstring-only by comparing each module's AST (docstrings normalised) against `main`.
