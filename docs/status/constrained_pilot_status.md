# Constrained-pilot gate — current status

Small living tracker for the `ISOLATED_SHADOW_LEDGER_V1` gate (A1–A5 + G1–G2)
defined in the two-contract readiness model
([docs/audits/2026-07-18-nxentra-current-state-audit.md](../audits/2026-07-18-nxentra-current-state-audit.md),
§21.2). This file records progress only; it does not restate or revise the audit.

Sequence: **A1+A2 → A4 → A3 → A5 → G1 → G2**.

| Item | Status | Notes |
|---|---|---|
| **A1** — cookie-JWT CSRF, four-mode auth matrix, explicit Shopify user binding | **Code-complete** at `8720887` (PR #106) | Operationally open until the live Shopify-iframe **G1** proof (App Bridge session tokens, `not_bound → link → login`, third-party cookies disabled). |
| **A2** — fail-closed production boot on unsafe test/bypass flags | **Complete** at `8720887` | Module-identity exemption; WSGI/ASGI/Celery assert the production settings module. |
| **A4** — constrained-pilot feature gates + Option B inventory invariant | **Reopened 2026-08-10** (previously Complete at `1e12250`, PR #107) — purchasing/AP breach (PR #118), runtime-admission serialization (PR #119), control-plane integrity (PR #120) and manual-journal EGP admission (PR #121, merged `9741a45`) all merged; Process-Surface Completeness Assessment **DONE** (2026-08-13). Still reopened for the residuals listed below (dispositions, not features) | Central `accounts.pilot_policy` runtime gates (per-platform webhook map, disputes, interactive-payout 403 vs scheduled skip, EGP-only ingestion, single-user/currency/fiscal freeze, `is_active` freeze + CI architecture rule + admin hardening); `activate_pilot_profile` (transactional, audited, fail-closed) + exhaustive `pilot_preflight` commands — go-live enforces the FULL agreed workflow (proven-EGP store, exact OWNER↔store binding, postable clearing/EBD mappings, active Paymob/Bosta provider + posting profile, canonical bank account). Option B forces NON_STOCK with no inventory/COGS mappings, no residual balances, and no FX cost fetch. Live G1 still required before real data. **A4 REOPENED (2026-08-10) — confirmed purchasing/AP-workflow breach:** the PR #117 documentation review surfaced that a pilot OWNER could enable the `purchases` module (via `CompanyModulesView.put` or a re-run of onboarding Step 4) and post NON_STOCK bills into full AP/expense/input-VAT journals; the purchases app held zero pilot references. PR #118 closes this confirmed breach: `Capability.PURCHASING_ACCOUNTING` enforced at three layers (module admission — `ModuleEnabled` + both enable doors; the `purchases` command boundary via a `requires_capability` gate on every command; `record_vendor_payment` with stable 403 propagation at its route) plus read-only preflight/activation drift detection on DURABLE canonical evidence — surviving documents, immutable purchase/AP events and vendor payment allocations, not only document rows (`purchases_module_enabled` / `purchase_document_state` / `purchase_financial_state`); manual journals, including vendor-tagged lines, are unchanged. **Activation/admission serialization (2026-08-11):** the fresh-review stale-profile race (a mutation admitted on a cached NONE profile committing AFTER a clean activation — reproduced on real PostgreSQL for all four purchasing-related families) is closed for PR #118's own paths: every covered mutation admits under the Company ADMISSION LOCK (`lock_company_for_admission`, FOR NO KEY UPDATE where supported, held through the outermost commit) while `activate_pilot_profile` keeps its explicit FOR UPDATE — exactly one serializable ordering, proven both ways by the PostgreSQL two-connection suite `tests/e2e/test_pilot_admission_serialization.py`; the four Company-event writer commands are Company-first aligned so no new Counter→Company AB/BA forms. **PR #118's merge (2026-08-11 at `87a8cf9`) closed the confirmed purchasing/AP breach and serialized its own admission paths only. The follow-up A4 Runtime Admission Serialization PR extends that closure to the older capability families — currency/fiscal (`configure_periods`), invitations / add-member (incl. the public multi-company `accept_invitation`), unsafe bank matching, Option-B inventory (`create_item`, and `update_item` with the Company admission lock HOISTED above the Item lock), Shopify payouts / disputes / payout-verify, legacy `/banking`, Stripe connect, and Shopify order/refund ingestion — each now admitting under the Company admission lock (`serialized_company_admission` / `lock_company_for_admission`, or the `requires_capability` decorator) held through commit, proven both orderings on PostgreSQL by `tests/e2e/test_a4_runtime_admission_serialization.py` and pinned by architecture Rules 12/12b. FOUR mutating paths are TRACKED DESIGN-DEFERRED RESIDUALS, deliberately NOT serialized here: second-company creation `register_signup` / `create_company` (Family B — deployment-wide creation, no existing Company row to admission-lock and no proven migration-free deployment-lock design), projection rebuild/replay `rebuild_for_company` (Family H — a multi-transaction drain that cannot retain admission ownership), and the scheduled/batched MULTI-COMMIT Stripe `sync.sync_payouts` and `import_settlement_csv` (which stay on their fail-closed point-in-time skip / up-front currency sweep, backed by defense-in-depth). NO global deadlock-freedom claim is made — the lock-order proofs are representative. A4 remains REOPENED until this follow-up merges AND the later Process-Surface Completeness Assessment completes. Full A4 re-closure additionally requires, after that follow-up, the read-only Pilot Process-Surface Completeness Assessment** covering the remaining optional modules, non-module financial entry points, posted-JE emitter families, and module-enable / direct-command / generic-ingest / operator-admin doors — the purchasing finding established that the prior negative-capability inventory was incomplete. **Process-Surface Completeness Assessment (2026-08-13, main `797572c`) — DONE (read-only):** 561 production processes inventoried across all door classes; 9 confirmed gaps (every one pilot-OWNER-reachable or operator-CLI — no anonymous/external corruption path survived adversarial verification), 5 refuted; preserved at `scratchpad/a4_process_surface_assessment.md` (audit/reference only, non-governing — live code overrides it). The assessment surfaced a restore / projection-apply / admin **control-plane** cluster (in-app backup restore could overwrite the whole company incl. currency/fiscal config while active; `CompanyProjection`/`UserProjection` applied caller-named fields generically — a second `pilot_profile` writer; `ProjectionAppliedEvent`/`EventBookmark` were admin-mutable — a `PROJECTION_REBUILD` bypass). The **A4 Control-Plane Integrity PR** (`fix/a4-control-plane-integrity`) closes **ONLY that cluster**: `Capability.BACKUP_RESTORE` blocks in-app restore under the Company admission lock (export/download stay available; G2 = a separate isolated-database drill); the identity projections apply only their command's whitelisted fields (never `pilot_profile`); `activate_pilot_profile` is pinned as the sole `pilot_profile` writer; the two projection-progress admins are read-only. **Control-Plane Integrity PR merged 2026-08-15 (`ab967bc`).** **Manual-Journal EGP Admission PR #121 — MERGED 2026-08-17 (`9741a45`) — closes ONLY the supported manual-journal EGP boundary from the Manual-Financial/EGP Boundary Assessment (2026-08-15):** the manual JE HTTP surface (create/update/save-complete/post/reverse) routes through thin process-boundary wrappers (`*_manual_journal_entry`) that own `serialized_company_admission` (Company admission lock held through the shared command's whole commit — exactly one serializable ordering with `activate_pilot_profile`, proven both orderings on PostgreSQL by `tests/e2e/test_a4_manual_journal_serialization.py`), while the canonical EGP validator `require_pilot_journal_currency` runs inside the shared commands at their currency-resolution points via a module-private, non-HTTP-forgeable process sentinel — header, every line, `amount_currency` foreign legs and non-1 exchange rates all reject BEFORE any event/sequence/row; provider/domain callers (Shopify, settlements, purchasing, inventory, reconciliation, EDIM, revaluation) keep their own boundaries and acquire no new lock; the dormant serializer write path is closed; preflight gains `non_egp_journal_line_data` / `fx_line_residue` / `fx_header_rate_residue` (all statuses, activation + drift modes); Rule 14 freezes the wrapper set, the sentinel producers and the COMPLETE dispositioned caller sets of every shared journal command. Disclosed correctness-first tradeoff: profile-NONE manual journals now serialize per-company on the admission lock (incl. any pre-existing foreign-post rate lookup running under it). **Still OPEN (A4 stays REOPENED):** manual AR (invoices/credit notes/receipts), EDIM financial commit, FX revaluation + exchange-rate maintenance doors, the fiscal-date commands, the clinic/properties vertical posture (founder decision), a projection-apply-layer coverage ratchet, and the four known design-deferred residuals (`register_signup`/`create_company`, `rebuild_for_company`, Stripe `sync.sync_payouts`, `import_settlement_csv`). The critical path is unchanged and the pilot remains blocked from merchant data. |
| **A3** — one central posted-JE invariant at emit + apply | **Open — A3-PR2 complete (PR #113, 2026-08-06); A3-PR2b MERGED (PR #116, 2026-08-09); A3-PR3 remains open and blocking** | **Complete:** A3-PR1 canonical posted-journal invariant core + read-only historical corpus scanner (merged at f03d90b); A3-PR2 emit-boundary enforcement at every JOURNAL_ENTRY_POSTED emit boundary (10 emit paths + external ingest) via `prepare_posted_journal_for_emit()` — exact prepared-payload emission, emit-only strict current-ledger representation, authoritative memo classification, owning-operation transaction rollback, external-ingest idempotency and concurrency hardening; the ±0.05 receipt/payment acceptance band is removed. **A3-PR2b — account-state serialization: MERGED (PR #116, 2026-08-09).** The serialized boundary `emit_posted_journal` (accounting/posted_journal_boundary.py) owns every JOURNAL_ENTRY_POSTED emission: for a newly inserted posted-journal event, all account facts used by canonical validation come from Account rows serialized against the CompanyEventCounter linearization point (Counter → pk-ordered Account rows → exact preparation → race-safe insert; live account mutations take Counter → Account; both idempotency layers — pre-lock lookup and post-validation-failure recheck — live inside the boundary). A3-PR2b is merged; **A3-PR3 must still land before deployment of the fresh pilot database and before real merchant data enters Nxentra.** **A3-PR3 apply/replay enforcement remains open and blocked** (production corpus scan or explicit compatibility decision) — historical events, rebuild and restore paths still carry no runtime protection. A3 overall is NOT complete. |
| **A5** — durable visible state for included money paths | **Open** | Not started. |
| **G1** — current-head fresh-company E2E (control totals + failure injection) | **Open** | The live operational proof for A1/A4. |
| **G2** — isolated-DB restore drill | **Open** | |

**Deployment reminder for A4:** the profile is set only via
`python manage.py activate_pilot_profile --company <id> --yes` (it refuses on any
forbidden state and never repairs data). Run
`python manage.py pilot_preflight --company <id> --phase go-live` before G1 and as
a drift check after each sync/import.

## Open architectural decisions

- **A3-PR2b — serialize account-state validation with posted-event emission (MERGED, PR #116, 2026-08-09).** Implemented as the serialized boundary `emit_posted_journal` plus Counter-first account mutations (`update_account`/`delete_account`), pk-ordered Account row locks, frozen AccountProjection update fields, and the four touched document-order corrections (vendor-payment bill locks, receipt-allocation invoice locks, credit-note original-invoice lock, platform DRAFT recovery → visible manual-review failure). Proven by the PostgreSQL concurrency suite tests/e2e/test_account_state_serialization.py. The sole fresh-review P2 (external `account.*` event-only mutation door: an ingested account event commits at sequence N while its row-apply lags, letting a journal at N+1 validate against pre-update facts) is closed by PROHIBITION — external ingest cannot mutate account state at all; see External-Ingest-Surface-Hardening below. The subsequent fresh-review P1 (the same forbidden history through the internal path: `update_account`/`delete_account` could commit their event while the fail-soft projection drain left the Account row unmaterialized — paused bookmark, handler error, or sync disabled) is closed FAIL-CLOSED: the command reports success only after proving the exact per-event ProjectionAppliedEvent marker AND the Account-row postcondition inside its owning transaction, else the entire attempt (event, consumed sequence, marker, row change, on_commit dispatch) rolls back with a visible CommandResult failure — mutation unavailability is accepted; contradictory financial history is not. Merged; A3-PR3 must still land before the fresh pilot database deployment and real merchant data.
- **Lock-Order Normalization (open; deferred availability hazards — NOT accepted financial-corruption paths).** The A3-PR2b lock-graph closure inventory found nine PRE-EXISTING opposing lock-order pairs on main that PR2b deliberately does not fix (it introduces none and removes four): JournalEntry (standalone save/post/reverse vs JournalEntryProjection under the sync drain); CompanySequence (standalone posting vs composed flows and projection JE builders); InventoryBalance / FifoLayer / StockLedgerSequenceCounter (standalone inventory commands vs composed sales/purchase/Shopify paths); ShopifyOrder (paid-stub upgrade vs handler stamp-back); ShopifyDispute (dispute-won update vs dispute-created handler); ShopifyFulfillment (4h sweep vs order-paid drain); ShopifyStore (OAuth saves vs sync_payouts); BankStatementLine (manual_match/resolve_difference vs ReconciliationProjection); JournalLine.reconciled (auto-match prepass vs projection). Consequence class: PostgreSQL may abort ONE transaction as a deadlock victim and roll it back atomically; the retry heals it — an availability/retry hazard, not a corruption path, and A3-PR2b makes no global deadlock-freedom claim. A5 must surface database deadlock/lock failures as visible retryable outcomes. This workstream becomes IMMEDIATELY active if any pilot observes a deadlock, and must complete before materially increasing per-company concurrency, shared multi-merchant database use, inventory re-enable, private beta, or GA. Accounting Kernel V1 is a natural migration point, but not the only trigger.
- **External-Ingest-Surface-Hardening (open; deferred — the `account.*` prohibition itself is ACTIVE, shipped with A3-PR2b).** The A3-PR2b correction closes the external account-mutation ordering hole by PROHIBITION: `events/ingest_policy.py` is the single definition of the reserved internal-event-type set (`account.created`, `account.updated`, `account.deleted` — the complete set of registered types whose projection mutates the Account rows feeding posted-journal validation); the runtime ingest boundary rejects them before any BusinessEvent lookup, financial lock, event emission or projection scheduling, with no financial persistence, regardless of key configuration (pre-existing rows and direct DB/admin edits cannot re-open the door; API-key authentication necessarily performs its key lookup and last_used_at touch first); `ExternalAPIKey.create_key` rejects them early as supplemental validation; an architecture meta-test pins the registered `account.*` namespace AND AccountProjection's consumed types to the reserved set, and the reserved set to registered types. Broader hardening of the generic ingest surface remains DEFERRED, without implementing now: replacing the broad registered-event surface (EVENT_DATA_CLASSES) with an explicit positive registry of externally ingestible contracts; source-system ↔ event-type compatibility; supported API-key creation/update validation and migration for existing rows; causal/materialization semantics for any future externally requested canonical-state mutation; operator-visible durable outcomes and retry handling for rejected/failed ingest. Activation triggers (any of): exposing generic ingest to third parties; onboarding another connector through generic ingest; permitting an external source to request canonical account or other command-owned state mutations; moving beyond the constrained isolated pilot contract. This deferral must NOT be used to weaken or postpone the active prohibition.
- **Inventory Books-Delta Foundation (open; blocking for inventory accounting, NOT for the constrained pilot).** The A3-PR2 root-cause assessment proved that correct inventory books amounts are a cross-workflow foundation, not per-call-site rounding — the partial raw-movement-quantization implementation was removed from PR #113 rather than presented as correct. Required scope: one provider-neutral inventory movement plan/apply boundary; the old-to-new STORED-books delta as the canonical books movement (GL inventory amount = `StockLedgerEntry.value_delta` = `InventoryBalance.stock_value` delta = inventory-event books movement); weighted-average costing; FIFO preview/apply and residue/shortfall semantics; `StockLedgerEntry` explicit-zero behavior; inventory adjustments and opening balances; sales-invoice COGS; Shopify fulfillment and refund restock; direct purchase bills; PO goods receipts versus supplier bills; GRNI/PPV policy; purchase credit notes; sales and purchase void stock effects; warehouse transfers; goods-receipt reversals; SQLite/PostgreSQL explicit-rounding parity. Until it lands it BLOCKS: re-enabling inventory accounting; onboarding any merchant relying on Nxentra inventory; any claim that inventory accounting is supported. It does NOT block the constrained Option B pilot — the pilot requires NON_STOCK items and no inventory/COGS accounting (A4 runtime gates + preflight residue detection keep those paths unreachable). Interim posture: inventory operations that cannot produce an exact current-ledger payload fail loudly at the canonical emit boundary rather than emitting unvalidated financial truth. Distinct from A3-PR2b (account-state serialization — blocks all real merchant data) and from the Precision Foundation below (global currency/precision schema decision).
- **Precision Foundation (future, pre-multi-currency).** The current two-decimal JournalLine schema and `Decimal("0.01")`/ROUND_HALF_EVEN posting quantum are the CONSTRAINED-PILOT (EGP-only) contract, not the permanent global policy. Before foreign-currency input is re-enabled, a non-EGP merchant is onboarded, multi-currency ships, or GA: decide ISO 4217 minor units, currency-aware quantization, money-field capacity/scale, quantity/unit-cost/exchange-rate/tax precision, rounding modes and accounts, calculation-vs-posting precision, display precision, and migration from the (18,2) schema. The controlled fresh-database reset may be the lowest-cost schema-widening point.
