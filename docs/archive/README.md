# Archived planning documents

Files in this directory preserve historical planning context; they are not the live roadmap. The current source of truth is [`NEXT_TASKS.md`](../../NEXT_TASKS.md).

`NEXT_TASKS_pre-cleanup_2026-07-11.md` is a byte-for-byte snapshot of the former root-level task file. Its relative links were authored from the repository root, so links inside that snapshot may resolve incorrectly from `docs/archive/`; interpret them as repository-root-relative paths. The file remains unchanged so historical wording and hashes stay auditable.
